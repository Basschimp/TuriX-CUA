# Windows dependencies for TuriX-CUA
# Install with: pip install -r requirements_windows.txt

# Core automation
pyautogui>=0.9.54
pygetwindow>=0.0.9
pymsgbox>=1.0.9
pytweening>=1.0.7
mouseinfo>=0.1.3

# Windows API access
pywin32>=306

# Optional: Enhanced UI automation (recommended for production)
# pywinauto>=0.6.8

# Common dependencies (also in main requirements.txt)
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
langchain-ollama>=0.1.0
pynput>=1.7.6
rapidfuzz>=3.6.0
pypinyin>=0.50.0

# OpenRouter support (uses OpenAI-compatible API)
openai>=1.0.0
